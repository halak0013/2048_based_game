//Bimillahirrahmanirrahim
package src;

public class MultiDimList<T> {
    private Node<T> head;

    void addBolck(T data, int x) {
        Node<T> newNode = new Node<T>(data, x);
        if (head == null) {
            head = newNode;
        } else {
            
        }
    }
}
